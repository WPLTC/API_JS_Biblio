<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bibliothèque - Liste des Livres</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    header {
      text-align: center;
      color: white;
      margin-bottom: 40px;
      padding: 30px 0;
    }

    header h1 {
      font-size: 3em;
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    header p {
      font-size: 1.2em;
      opacity: 0.9;
    }

    .stats {
      background: white;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 30px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      text-align: center;
    }

    .stats h2 {
      color: #667eea;
      font-size: 1.5em;
    }

    .books-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 25px;
      margin-bottom: 30px;
    }

    .book-card {
      background: white;
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.2);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .book-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, #667eea, #764ba2);
    }

    .book-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 24px rgba(0,0,0,0.3);
    }

    .book-title {
      font-size: 1.4em;
      font-weight: bold;
      color: #333;
      margin-bottom: 10px;
      line-height: 1.3;
    }

    .book-author {
      color: #667eea;
      font-size: 1.1em;
      margin-bottom: 15px;
      font-weight: 600;
    }

    .book-details {
      margin-top: 15px;
      padding-top: 15px;
      border-top: 1px solid #eee;
    }

    .book-detail-item {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
      font-size: 0.95em;
    }

    .book-detail-label {
      color: #666;
      font-weight: 500;
    }

    .book-detail-value {
      color: #333;
    }

    .book-description {
      color: #555;
      font-size: 0.9em;
      line-height: 1.6;
      margin-top: 15px;
      font-style: italic;
    }

    .badge {
      display: inline-block;
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.85em;
      font-weight: 600;
      margin-top: 10px;
    }

    .badge-available {
      background: #10b981;
      color: white;
    }

    .badge-unavailable {
      background: #ef4444;
      color: white;
    }

    .empty-state {
      background: white;
      border-radius: 15px;
      padding: 60px 20px;
      text-align: center;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .empty-state h2 {
      color: #667eea;
      font-size: 2em;
      margin-bottom: 15px;
    }

    .empty-state p {
      color: #666;
      font-size: 1.1em;
    }

    .error-message {
      background: #fee;
      border: 2px solid #fcc;
      border-radius: 10px;
      padding: 20px;
      color: #c33;
      text-align: center;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>Bibliothèque</h1>
      <p>Découvrez notre collection de livres</p>
    </header>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error-message">
        <strong>Erreur:</strong> <%= error %>
      </div>
    <% } %>

    <div class="stats">
      <h2>Total de livres: <%= totalBooks %></h2>
    </div>

    <% if (books && books.length > 0) { %>
      <div class="books-grid">
        <% books.forEach(book => { %>
          <div class="book-card">
            <div class="book-title"><%= book.title %></div>
            <div class="book-author"> <%= book.author.name || 'Auteur inconnu' %></div>
            
            <div class="book-details">
              <div class="book-detail-item">
                <span class="book-detail-label">ISBN:</span>
                <span class="book-detail-value"><%= book.isbn %></span>
              </div>
              <div class="book-detail-item">
                <span class="book-detail-label">Année:</span>
                <span class="book-detail-value"><%= book.published_year %></span>
              </div>
              <% if (book.pages) { %>
                <div class="book-detail-item">
                  <span class="book-detail-label">Pages:</span>
                  <span class="book-detail-value"><%= book.pages %></span>
                </div>
              <% } %>
            </div>

            <% if (book.description) { %>
              <div class="book-description">
                "<%= book.description %>"
              </div>
            <% } %>

            <span class="badge <%= book.available !== false ? 'badge-available' : 'badge-unavailable' %>">
              <%= book.available !== false ? '✓ Disponible' : '✗ Indisponible' %>
            </span>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <h2>📖 Aucun livre disponible</h2>
        <p>La bibliothèque est vide pour le moment.</p>
      </div>
    <% } %>
  </div>
</body>
</html>
