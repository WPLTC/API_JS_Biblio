<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bibliothèque - Liste des Livres</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    header {
      text-align: center;
      color: white;
      margin-bottom: 40px;
      padding: 30px 0;
    }

    header h1 {
      font-size: 3em;
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    header p {
      font-size: 1.2em;
      opacity: 0.9;
    }

 
    .search-container {
      background: white;
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 30px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .search-form {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      justify-content: center;
    }

    .search-input {
      flex: 1;
      min-width: 250px;
      padding: 12px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 16px;
    }

    .search-select {
      padding: 12px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 16px;
      background: white;
    }

    .search-btn {
      padding: 12px 25px;
      background: #667eea;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 16px;
      font-weight: 600;
      transition: background 0.3s;
    }

    .search-btn:hover {
      background: #5a6fd6;
    }

    .reset-link {
      display: flex;
      align-items: center;
      color: #666;
      text-decoration: none;
      font-size: 0.95em;
    }
    .reset-link:hover { text-decoration: underline; }

  
    .stats {
      background: white;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 30px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      text-align: center;
    }

    .stats h2 {
      color: #667eea;
      font-size: 1.5em;
    }

    /* CSS GRILLE */
    .books-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 25px;
      margin-bottom: 30px;
    }

    .book-card {
      background: white;
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.2);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .book-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, #667eea, #764ba2);
    }

    .book-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 24px rgba(0,0,0,0.3);
    }

    .book-title {
      font-size: 1.4em;
      font-weight: bold;
      color: #333;
      margin-bottom: 10px;
      line-height: 1.3;
    }

    .book-author {
      color: #667eea;
      font-size: 1.1em;
      margin-bottom: 15px;
      font-weight: 600;
    }

    .book-details {
      margin-top: 15px;
      padding-top: 15px;
      border-top: 1px solid #eee;
    }

    .book-detail-item {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
      font-size: 0.95em;
    }

    .book-detail-label {
      color: #666;
      font-weight: 500;
    }

    .book-detail-value {
      color: #333;
    }

    .book-description {
      color: #555;
      font-size: 0.9em;
      line-height: 1.6;
      margin-top: 15px;
      font-style: italic;
    }


    .badge {
      display: inline-block;
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.85em;
      font-weight: 600;
      margin-top: 10px;
    }

    .badge-available { background: #10b981; color: white; }
    .badge-unavailable { background: #ef4444; color: white; }

    
    .empty-state {
      background: white;
      border-radius: 15px;
      padding: 60px 20px;
      text-align: center;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .empty-state h2 {
      color: #667eea;
      font-size: 2em;
      margin-bottom: 15px;
    }

    .empty-state p { color: #666; font-size: 1.1em; }

    .error-message {
      background: #fee;
      border: 2px solid #fcc;
      border-radius: 10px;
      padding: 20px;
      color: #c33;
      text-align: center;
      margin-bottom: 20px;
    }

    /* CSS PAGINATION */
    .pagination {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 15px;
      margin-top: 20px;
      padding: 20px;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .page-btn {
      padding: 8px 16px;
      background: white;
      border: 1px solid #ddd;
      border-radius: 5px;
      color: #667eea;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s;
    }

    .page-btn:hover {
      background: #f8f9fa;
      border-color: #667eea;
    }

    .page-info {
      color: #555;
      font-weight: 500;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>Bibliothèque</h1>
      <p>Découvrez notre collection de livres</p>
    </header>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error-message">
        <strong>Erreur:</strong> <%= error %>
      </div>
    <% } %>

    <div class="search-container">
      <form action="/" method="GET" class="search-form">
        <input 
          type="text" 
          name="search" 
          class="search-input"
          placeholder="Titre ou nom de l'auteur..." 
          value="<%= (typeof searchParams !== 'undefined' && searchParams.search) ? searchParams.search : '' %>"
        >
        
        <select name="available" class="search-select">
          <option value="">Tous les statuts</option>
          <option value="true" <%= (typeof searchParams !== 'undefined' && searchParams.available === 'true') ? 'selected' : '' %>>Disponible</option>
          <option value="false" <%= (typeof searchParams !== 'undefined' && searchParams.available === 'false') ? 'selected' : '' %>>Indisponible</option>
        </select>

        <button type="submit" class="search-btn">Rechercher</button>
        
        <% if ((typeof searchParams !== 'undefined' && searchParams.search) || (typeof searchParams !== 'undefined' && searchParams.available)) { %>
          <a href="/" class="reset-link">Réinitialiser</a>
        <% } %>
      </form>
    </div>

    <div class="stats">
      <h2>Total de livres affichés : <%= totalBooks %></h2>
    </div>

    <% if (books && books.length > 0) { %>
      <div class="books-grid">
        <% books.forEach(book => { %>
          <div class="book-card">
            <div class="book-title"><%= book.title %></div>
            <div class="book-author"> <%= book.author.name || 'Auteur inconnu' %></div>
            
            <div class="book-details">
              <div class="book-detail-item">
                <span class="book-detail-label">ISBN:</span>
                <span class="book-detail-value"><%= book.isbn %></span>
              </div>
              <div class="book-detail-item">
                <span class="book-detail-label">Année:</span>
                <span class="book-detail-value"><%= book.published_year %></span>
              </div>
              <% if (book.pages) { %>
                <div class="book-detail-item">
                  <span class="book-detail-label">Pages:</span>
                  <span class="book-detail-value"><%= book.pages %></span>
                </div>
              <% } %>
            </div>

            <% if (book.description) { %>
              <div class="book-description">
                "<%= book.description %>"
              </div>
            <% } %>

            <span class="badge <%= book.available !== false ? 'badge-available' : 'badge-unavailable' %>">
              <%= book.available !== false ? '✓ Disponible' : '✗ Indisponible' %>
            </span>
          </div>
        <% }); %>
      </div>

      <% if (totalPages > 1) { %>
        <div class="pagination">
          <% if (currentPage > 1) { %>
            <a href="/?page=<%= currentPage - 1 %>&search=<%= searchParams.search || '' %>&available=<%= searchParams.available || '' %>" class="page-btn">
              ← Précédent
            </a>
          <% } %>

          <span class="page-info">
            Page <%= currentPage %> sur <%= totalPages %>
          </span>

          <% if (currentPage < totalPages) { %>
            <a href="/?page=<%= currentPage + 1 %>&search=<%= searchParams.search || '' %>&available=<%= searchParams.available || '' %>" class="page-btn">
              Suivant →
            </a>
          <% } %>
        </div>
      <% } %>

    <% } else { %>
      <div class="empty-state">
        <h2>📖 Aucun livre trouvé</h2>
        <p>Essayez de modifier vos critères de recherche.</p>
        <br>
        <a href="/" class="search-btn" style="text-decoration: none; font-size: 0.9em;">Voir tous les livres</a>
      </div>
    <% } %>
  </div>
</body>
</html>